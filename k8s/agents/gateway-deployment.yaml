apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-gateway
  namespace: hr-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agent-gateway
  template:
    metadata:
      labels:
        app: agent-gateway
    spec:
      serviceAccountName: hr-ai-sa   # allows kubectl scale from inside pod
      containers:
      - name: backend
        image: hr-ai-backend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: MOCK_LLM
          value: "false"
        - name: MLX_LM_BASE_URL
          value: "http://mlx-lm-service:8080/v1"
        - name: LLM_MODEL
          value: "qwen2.5:1.5b"
        - name: REDIS_URL
          value: "redis://redis-service:6379/0"
        - name: DATABASE_URL
          value: "sqlite+aiosqlite:///./hrdb.sqlite3"
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-gateway-service
  namespace: hr-ai
spec:
  selector:
    app: agent-gateway
  ports:
  - port: 80
    targetPort: 8000
